policy "permit-clinician-redacted"
permit
  action == "readRedacted";
  resource == "redacted";
  <jwt.token>.payload.user_role == "CLINICIAN";
obligation
  {
    "type": "redactFields",
    "fields": ["ssn", "creditCard"]
  }
