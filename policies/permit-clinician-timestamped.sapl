policy "permit-clinician-timestamped"
permit
  action == "readTimestamped";
  resource == "timestamped";
  <jwt.token>.payload.user_role == "CLINICIAN";
obligation
  {
    "type": "injectTimestamp"
  }
